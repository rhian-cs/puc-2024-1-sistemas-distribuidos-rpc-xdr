## Installation

Install dependencies:

```sh
sudo apt install libntirpc-dev portmap
```

Compile C programs:

```
make
```

## Usage

In two separate terminals, run:

```sh
./server
./client 127.0.0.1
```
